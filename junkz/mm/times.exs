
    defmodule Times do
        @moduledoc """
          Documentation for Times
        """

        @doc """

        # Example
        """

        def double(n), do: n * 2
        def triple(n), do: n * 3
        def quadruple(n), do: double(n) * double(n)
    end
